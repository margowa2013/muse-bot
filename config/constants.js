// Привітальні повідомлення (рандомні, крім першого запуску)
const GREETING_MESSAGES = [
    "Привіт, коханий ❤ … Тут ти можеш замовити турботу, ніжність або пригоди…",
];

// Фіксоване повідомлення при першому запуску
const FIRST_TIME_MESSAGE = "Любовна система активована 💕\n\nMuse online і готова прочитати ваші побажання 💌";

// Стартове повідомлення (не використовується, залишено для сумісності)
const START_MESSAGE = "Привіт, коханий ❤ … Тут ти можеш замовити турботу, ніжність або пригоди…";

// Емодзі для категорій
const CATEGORY_EMOJIS = {
    'Їжа': '🍽️',
    'Побачення': '🎬',
    'Приємності': '💝',
    'Коли на відстані': '📱',
    'Кошик': '🛒',
    'Мій рахунок': '💳'
};

// ID двох користувачів для комунікації
// Вкажіть тут Telegram user ID обох людей
// Коли один оформлює замовлення, інший отримає повідомлення
const USER_IDS = {
    USER_1: process.env.USER_1_ID ? parseInt(process.env.USER_1_ID) : null, // Перший користувач
    USER_2: process.env.USER_2_ID ? parseInt(process.env.USER_2_ID) : null  // Другий користувач
};

// Функція для отримання ID другого користувача
function getPartnerId(userId) {
    if (USER_IDS.USER_1 === userId) {
        return USER_IDS.USER_2;
    } else if (USER_IDS.USER_2 === userId) {
        return USER_IDS.USER_1;
    }
    return null;
}

// Функція для перевірки, чи користувач є одним з двох
function isCoupleUser(userId) {
    return USER_IDS.USER_1 === userId || USER_IDS.USER_2 === userId;
}

module.exports = {
    GREETING_MESSAGES,
    FIRST_TIME_MESSAGE,
    START_MESSAGE,
    CATEGORY_EMOJIS,
    USER_IDS,
    getPartnerId,
    isCoupleUser
};
